# Система Гео-оповещений (Geo Broadcasting System)

Бэкенд-сервис, реализующий ядро системы гео-оповещений. Сервис предоставляет API для управления "инцидентами" (опасными зонами), позволяет мобильным приложениям проверять местоположение пользователей относительно этих зон и асинхронно уведомляет внешние системы через вебхуки при вхождении пользователя в опасную зону.

## 🚀 Технологический стек

-   **Go 1.25.5**
-   **PostgreSQL 15 + PostGIS**: Для хранения данных и выполнения эффективных гео-запросов.
-   **Redis**: Используется как для кэширования, так и в качестве брокера сообщений для асинхронной отправки вебхуков.
-   **Gin**: В качестве HTTP-фреймворка.
-   **Docker & Docker Compose**: Для контейнеризации и легкого развертывания окружения для разработки.
-   **Swagger**: Для документирования API.

## ⚙️ Начало работы

### 1. Предварительные требования

-   **Docker** и **Docker Compose** (V2).
-   **Go 1.25+** (если планируется запуск без Docker).
-   **`ngrok`** (опционально, для тестирования вебхуков).

### 2. Конфигурация

Скопируйте файл `.env.example` и создайте на его основе свой файл `.env`.

```bash
cp .env.example .env
```

Файл `.env` содержит все необходимые переменные окружения. **Для запуска в Docker изменять стандартные значения `DATABASE_URL` и `REDIS_ADDR` не нужно**, так как они уже настроены для внутренней сети Docker.

-   `API_KEYS`: Укажите через запятую ваши секретные ключи для доступа к API.
-   `WEBHOOK_URL`: URL, на который будут отправляться вебхуки.
-   `NGROK_AUTHTOKEN` (если вы планируете использовать ngrok в Docker): Ваш токен авторизации ngrok.

### 3. Запуск с Docker Compose (рекомендуемый способ)

Этот способ является основным и самым простым для запуска полного окружения (Go-приложение, PostgreSQL, Redis).

1.  **Сборка и запуск контейнеров:**
    Выполните команду в корневой директории проекта:
    ```bash
    docker compose up --build -d
    ```
    Эта команда соберет образ вашего Go-приложения и запустит все сервисы в фоновом режиме.

2.  **Проверка статуса:**
    Убедитесь, что все контейнеры успешно запустились:
    ```bash
    docker compose ps
    ```

3.  **Просмотр логов:**
    Для просмотра логов конкретного сервиса (например, вашего приложения):
    ```bash
    docker compose logs -f app
    ```

4.  **Остановка:**
    Для остановки и удаления всех контейнеров:
    ```bash
    docker compose down
    ```

### 4. Миграции базы данных

Миграции применяются **автоматически** при каждом запуске `app`-контейнера. Вам не нужно выполнять их вручную при использовании Docker.

## 📡 API Endpoints

Сервис будет доступен по адресу `http://localhost:8080`.

### Swagger-документация

Интерактивная документация API (Swagger UI) доступна по адресу:
[http://localhost:8080/swagger/index.html](http://localhost:8080/swagger/index.html)

### Аутентификация

Все эндпоинты, кроме `/location/check` и `/system/health`, требуют аутентификации. Передавайте ваш API-ключ в заголовке `X-API-Key`.

### Примеры запросов

(Замените `[incident_uuid]` на реальный ID инцидента и `my-secret-api-key-1` на ваш ключ)

-   **Создать инцидент:**
    ```bash
    curl -X POST http://localhost:8080/api/v1/incidents \
      -H "Content-Type: application/json" \
      -H "X-API-Key: my-secret-api-key-1" \
      -d '{"name": "Зона подтопления", "latitude": 55.75, "longitude": 37.61, "radius_meters": 2000, "status": "active"}'
    ```

-   **Получить список инцидентов (с пагинацией):**
    ```bash
    curl "http://localhost:8080/api/v1/incidents?page=1&pageSize=5" \
      -H "X-API-Key: my-secret-api-key-1"
    ```

-   **Обновить инцидент:**
    ```bash
    curl -X PUT http://localhost:8080/api/v1/incidents/[incident_uuid] \
      -H "Content-Type: application/json" \
      -H "X-API-Key: my-secret-api-key-1" \
      -d '{"name": "Обновленная зона", "latitude": 55.75, "longitude": 37.61, "radius_meters": 2500, "status": "active"}'
    ```

-   **Деактивировать инцидент:**
    ```bash
    curl -X DELETE "http://localhost:8080/api/v1/incidents/[incident_uuid]" \
      -H "X-API-Key: my-secret-api-key-1"
    ```

-   **Проверить геолокацию пользователя:**
    ```bash
    curl -X POST http://localhost:8080/api/v1/location/check \
      -H "Content-Type: application/json" \
      -d '{"user_id": "user-123", "latitude": 55.751, "longitude": 37.615}'
    ```

-   **Получить статистику:**
    ```bash
    curl "http://localhost:8080/api/v1/incidents/stats" \
      -H "X-API-Key: my-secret-api-key-1"
    ```

## 🎣 Тестирование Вебхуков с `ngrok`

Для полноценного тестирования отправки вебхуков необходимо, чтобы ваш локальный сервис, принимающий вебхуки, был доступен из контейнера `app` через публичный URL. `ngrok` идеально подходит для этой задачи.

1.  **Запустите `webhook-receiver`:**
    В `docker-compose.yml` уже есть сервис `webhook-receiver` (на базе `hashicorp/http-echo`), который слушает порт `9090` и выводит в лог все входящие запросы. Убедитесь, что он запущен вместе с остальными контейнерами.

2.  **Запустите `ngrok`:**
    Откройте новый терминал на вашем компьютере и выполните команду, чтобы создать туннель к порту `9090`:
    ```bash
    ngrok http 9090
    ```

3.  **Настройте `WEBHOOK_URL`:**
    `ngrok` выдаст вам публичный URL (например, `https://<random-string>.ngrok-free.app`). Скопируйте его и вставьте в ваш `.env` файл:
    ```ini
    WEBHOOK_URL=https://<random-string>.ngrok-free.app
    ```

4.  **Перезапустите сервис `app`:**
    Чтобы приложение подхватило новый URL, выполните быстрый перезапуск только этого контейнера:
    ```bash
    docker compose restart app
    ```

5.  **Проведите тест:**
    Отправьте запрос на `/api/v1/location/check`, который должен спровоцировать отправку вебхука.

6.  **Проверьте результат:**
    Посмотрите логи контейнера `webhook-receiver`. Вы должны увидеть запись о входящем `POST` запросе.
    ```bash
    docker compose logs webhook-receiver
    ```
    Вы также можете инспектировать трафик в веб-интерфейсе `ngrok`, который обычно доступен по адресу `http://localhost:4040`.

## 🧪 Запуск тестов

Для запуска unit-тестов выполните:
```bash
go test ./...
```